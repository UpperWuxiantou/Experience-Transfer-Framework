# 基于经验推广框架的快速训练预测 Agent

## 概述

非对话模式的快速工具 Agent。流程固定：**从文件学习 → 预测输出**，不做闲聊。

## 前提

运行前确认项目目录下的 `.env` 文件已填入你的 API Key（参考[框架说明](经验推广框架.md)）。

## 运行

```bash
python "基于经验推广框架的快速训练预测agent.py"
```

## 工作流程

```
 1. 询问训练文件路径
       ↓
 2. 从文件学习经验（learn_from_file）
       ↓
 3. 询问要预测的场景
       ↓
 4. 输出预测结果（带思考过程）
       ↓
 5. 重复 3-4，直到退出
```

## 运行示例

```
========================================================
  经验推广框架 —— 快速训练预测 Agent
  模式：文件学习 → 预测输出
========================================================

请输入训练文件路径: 使用说明.md
  ✓ 已从文档学习 5 条经验（共 5 条）

知识库就绪：5 条经验，16 个实体

请输入要预测的场景（输入 quit 退出）: 我在野外遇到狼群
  ✓ 场景元素：我 → 遇到 → 狼
  ▶ 分析特征...
  ▶ 联网搜索...
  ▶ 属性传播匹配...
  ▶ 生成建议...
（输出预测结果）

请输入要预测的场景（输入 quit 退出）: 遇到一条毒蛇
...
```

## 技术说明

- 两个阶段分离：文件学习阶段（成功后退出循环）→ 预测阶段（可多次预测）
- 预测使用 `predict_stream(verbose=True)`，输出思考过程 + 推理结果
- 输入 `quit` / `exit` / `q` 退出
