# 基于经验推广框架的用户交互 Agent

## 概述

智能对话 Agent，像聊天一样自然交互。底层自动识别用户意图，调用框架的学习、预测或聊天功能。

## 前提

运行前确认项目目录下的 `.env` 文件已填入你的 API Key（参考[框架说明](经验推广框架.md)）。

## 运行

```bash
python "基于经验推广框架的用户交互agent.py"
```

## 交互方式

无需记忆命令，像聊天一样输入即可。系统自动识别四种意图：

| 用户输入 | 意图 | 行为 |
|----------|------|------|
| "我刚才在楼道里遇到一只野猫，它突然抓了我一下" | `learn` | LLM 自然回应 + 后台存入知识库 |
| "读一下使用说明 把里面的经验学了" | `learn_file` | 提取文件路径 → 批量学习 |
| "如果我在野外遇到狼群怎么办？" | `predict` | 属性分析传播 → LLM 推理输出 |
| "你好，今天天气不错" | `chat` | 正常聊天对话 |

运行示例：

```
>>> 我刚才在山上遇到野猪了，它冲过来撞我
（LLM 自然回应...）
  ✓ 已存入经验库（共 1 条经验，4 个实体）

>>> 如果我在野外遇到老虎怎么办？
  ✓ 场景元素：我 → 遇到 → 老虎
  ▶ 分析特征...
  ▶ 联网搜索...
  ▶ 属性传播匹配...
  ▶ 生成建议...
（LLM 基于分析结果给出自然建议）

>>> 再见
退出
```

## 技术说明

- 意图分类通过 LLM 极简 prompt 完成（< 50 token），低延迟
- `chat` 为兜底分类，即使模糊输入也不会报错
- 任何建议性问题都会触发 `predict` 模块，不会用聊天模式敷衍
- 输入 `quit` / `exit` / `q` 退出
