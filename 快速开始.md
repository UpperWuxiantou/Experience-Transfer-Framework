# 快速开始

5 分钟内体验效果。

## 1. 安装依赖

```bash
pip install openai python-dotenv duckduckgo_search
```

## 2. 配置 API Key

打开项目目录下的 `.env` 文件，填入你的 API Key：

```
LLM_API_KEY=sk-你的key
LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
```

想换其他 LLM？改三行配置即可（示例换用 Ollama 本地模型）：

```
LLM_API_KEY=ollama
LLM_MODEL=qwen2.5:7b
LLM_BASE_URL=http://localhost:11434/v1
```

## 3. 运行体验

### 方式一：智能聊天

```bash
python "基于经验推广框架的用户交互agent.py"
```

直接聊天，系统自动学习：

```
>>>
```

### 方式二：文件学习 → 预测

```bash
python "基于经验推广框架的快速训练预测agent.py"
```

按提示输入文件路径，然后输入场景预测。

### 方式三：Python 代码调用

```python
from my_llm import LLM
from 经验推广框架 import ExperienceAgent

llm = LLM()
agent = ExperienceAgent(llm)

# 输入 2 条经验
agent.learn("在丛林里遇到老虎，老虎扑过来咬伤了我。")
agent.learn("看到远处有老虎，我悄悄绕开，安全离开。")

# 预测新场景
for chunk in agent.predict_stream("我独自在野外遇到了一群狼", verbose=True):
    print(chunk, end="", flush=True)
```

## 4. 第二次运行

知识库会自动保存。关闭程序再打开，以前学过的经验还在，可以直接继续预测。

```bash
python "基于经验推广框架的用户交互agent.py"
# 知识库已恢复，直接问预测问题即可
```

## 5. 试试用自己的数据

准备一个 `.md` 或 `.txt` 文件，里面写几条事件描述：

```markdown
面试时自信地回答了所有问题，面试官当场给了通过。
项目 deadline 前熬夜赶工，交付时出现了多个 bug。
```

然后运行快速训练预测 agent，输入这个文件路径，再用类似场景测试预测效果。
