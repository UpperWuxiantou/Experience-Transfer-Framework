# 经验推广框架

基于用户经验 + 联网属性增强的推理引擎。让 Agent 从用户提供的经验中学习，通过**属性共现传播**机制对新场景进行推理预测，不依赖 LLM 训练语料中的统计记忆。

---

## 目录

- [整体架构](#整体架构)
- [快速开始](#快速开始)
- [模块详解](#模块详解)
  - [1. 数据结构层](#1-数据结构层)
  - [2. 知识库——属性传播引擎](#2-知识库属性传播引擎)
  - [3. 属性归一化](#3-属性归一化)
  - [4. LLM 提取层](#4-llm-提取层)
  - [5. 联网属性搜索](#5-联网属性搜索)
  - [6. Agent 主类](#6-agent-主类)
- [安装与配置](#安装与配置)
- [API 参考](#api-参考)
- [核心机制专题](#核心机制专题)
- [完整示例](#完整示例)

---

## 整体架构

### 文件结构

```
agent经验推广框架/
├── my_llm.py                          # LLM 交互封装
├── 经验推广框架.py                     # 框架核心（纯推理引擎）
├── 基于经验推广框架的用户交互agent.py   # 智能对话 Agent
├── 基于经验推广框架的快速训练预测agent.py # 快速训练预测工具
├── 经验推广框架.md
├── .env
└── llm交互封装模块&经典智能体框架实例/
    ├── my_llm.py
    └── my_agent_demo.py
```

### 核心流程

```
学习阶段 (learn)：
  用户事件描述 → SAOR 提取 → 属性生成 → 知识库注册 → 更新共现图 → 属性归一化

预测阶段 (predict)：
  场景描述 → SAO 提取 → 属性生成 → 联网补充属性 → 合并传播 → LLM 推理
```

### 模块依赖关系

```
my_llm.py              ← LLM API 封装（底层）
    ↑
经验推广框架.py         ← 框架核心（依赖 my_llm）
    ↑
基于经验推广框架的*.py  ← 具体 Agent（依赖 经验推广框架）
```

---

## 快速开始

```python
from my_llm import LLM
from 经验推广框架 import ExperienceAgent

llm = LLM()
agent = ExperienceAgent(llm)

# 1. 学习经验
agent.learn("在丛林里遇到老虎，老虎扑过来咬伤了我。")
agent.learn("看到远处有老虎，我悄悄绕开，安全离开。")

# 2. 预测新场景
result = agent.predict("我独自在野外遇到了一群狼")
print(result)

# 3. 流式预测（逐 token 输出，带思考过程）
for chunk in agent.predict_stream("我独自在野外遇到了一群狼", verbose=True):
    print(chunk, end="", flush=True)

# 4. 查看知识库
print(agent.show_knowledge())
```

---

## 模块详解

### 1. 数据结构层

位于 `经验推广框架.py` 第 39-61 行。

#### Entity（实体）

实体是属性空间中的基本单位。主体、动作、客体、结果全部表示为 Entity。

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | `str` | 实体名称 | `"老虎"` |
| `attrs` | `Dict[str, float]` | 属性名 → 显著度(0~1) | `{"食肉": 1.0, "有尖牙": 1.0}` |
| `source_weights` | `Dict[str, float]` | 每个属性的来源权重 | `{"食肉": 1.0}` |

```python
@dataclass
class Entity:
    name: str
    attrs: Dict[str, float] = field(default_factory=dict)
    source_weights: Dict[str, float] = field(default_factory=dict)
```

#### Experience（经验）

完整经验 = 四元组（主体、动作、客体、结果），每个元素都是一个 Entity 对象。

```python
@dataclass
class Experience:
    subject: Entity
    action: Entity
    object: Entity
    result: Entity
```

#### AttrMatch（属性匹配结果）

属性传播的输出结构，回答"这个属性是怎么来的"。

| 字段 | 说明 | 示例 |
|------|------|------|
| `attr_name` | 属性名 | `"攻击力高"` |
| `value` | 属性值（经传播衰减） | `0.67` |
| `source_weight` | 来源权重（固定） | `1.0` |
| `source_attr` | 从哪个属性传播而来 | `"有尖牙"` |
| `propagation_distance` | 传播距离（0=直接匹配） | `0.33` |
| `confidence` | 可信度 = 共现率 × 来源权重 | `0.67` |

---

### 2. 知识库——属性传播引擎

位于 `经验推广框架.py` 第 68-138 行。知识库维护两张核心表：

- **实体表**：`entity_name → {attr: value}`
- **属性共现表**：`(attr_a, attr_b) → 共现概率 P`

#### 注册实体

```python
def register(self, name: str, attrs: Dict[str, float],
             source_weight: float = 1.0) -> Entity:
```

增量式注册，新属性和已有属性合并。`source_weight` 取最大值（同一属性从用户经验和联网同时获得时，取用户经验的 1.0）。

#### 更新共现图

```python
def update_cooccurrence(self):
```

每次从头重建共现表，确保归一化后旧的属性名不残留。共现率计算方法：

```
共现率 = min(P(a1|a2), P(a2|a1))
```

取两个条件概率中的较小值作为保守估计。

#### 属性传播

```python
def propagate(self, query_attrs: Dict[str, float],
              source_weights: Dict[str, float] = None,
              max_distance: float = 0.3,
              max_results: int = 15) -> List[AttrMatch]:
```

核心传播逻辑分四步：

**第一步**：遍历每个查询属性，添加直接匹配项（`distance = 0.0`）

**第二步**：在共现表中查找与查询属性共现的其他属性，计算传播后的属性值：

```
传播距离 = 1 - 共现率
传播后的属性值 = 原始属性值 × 共现率
confidence = 共现率 × source_weight
```

**第三步**：只采纳 `传播距离 ≤ max_distance（0.30）` 的结果

**第四步**：按距离从小到大、置信度从大到小排序，取前 `max_results` 条

#### 传播示例

```
query_attrs = {"群居": 1.0, "食肉": 1.0, "有尖牙": 1.0}

知识库共现表：
("有尖牙", "攻击力高"): 0.67   → 距离 0.33
("食肉", "危险"): 1.0          → 距离 0.00

传播结果：
- 群居=1.00   (直接)
- 食肉=1.00   (直接)
- 有尖牙=1.00 (直接)
- 危险=1.00   (←食肉，距离=0.00)   ← 完全可靠
- 攻击力高=0.67 (←有尖牙，距离=0.33) ← 可参考
```

---

### 3. 属性归一化

位于 `经验推广框架.py` 第 145-242 行。解决"同一概念不同名称"问题。

#### 为什么需要归一化

```
LLM 第一次提取老虎：{"尖牙锋利度": 0.99, "前肢攻击力": 0.95}
LLM 第二次提取老虎：{"尖牙致伤能力": 0.95, "肌肉爆发力": 0.88}

归一化前："尖牙锋利度"和"尖牙致伤能力"是两个不同属性，传播失效
归一化后：统一为"尖牙致伤能力"，传播匹配率大幅提升
```

#### 归一化流程

1. 收集知识库所有属性名
2. 前缀清理（去掉 `"- "`、`"1. "` 等污染）
3. 调用 LLM 将同义属性归为一组（JSON 格式输出）
4. 构建 `synonym → canonical` 映射
5. 链式解析映射（支持多层：`尖牙锋利度 → 尖牙锐利度 → 尖牙致伤能力`）
6. 应用到所有实体并重建共现表

#### 合并规则

| 规则 | 示例 |
|------|------|
| "有X" 和 "X" 视为相同 | `"有尖牙"` → `"尖牙"` |
| "X能力" 和 "X" 视为相同 | `"攻击能力"` → `"攻击"` |
| "X性" 和 "X" 视为相同 | `"危险性"` → `"危险"` |
| "- X" 去掉前缀 | `"- 速度"` → `"速度"` |
| "N. X" 去掉编号 | `"1. 冲击力"` → `"冲击力"` |

---

### 4. LLM 提取层

位于 `经验推广框架.py` 第 249-366 行。负责从文本中提取结构化信息。

#### 四个 Prompt

| Prompt | 用途 | 输出 |
|--------|------|------|
| `SAO_PROMPT` | 提取三元组（预测用） | `{"subject", "action", "object"}` |
| `SAOR_PROMPT` | 提取四元组（学习用） | `{"subject", "action", "object", "result"}` |
| `ATTR_PROMPT` | 为实体生成属性（参考已有属性名） | `属性名: 数值` 逐行 |
| `WEB_ATTR_PROMPT` | 从网络信息提取属性 | `属性名: 数值` 逐行 |

#### known_attrs 机制

属性生成时传入知识库已有的属性名列表，鼓励 LLM 保持命名一致。这是解决命名不一致的第一道防线（归一化是第二道）。

#### 属性解析

```python
def _parse_attrs(self, text: str, max_attrs: int) -> Dict[str, float]:
```

两个关键细节：
1. `split(":", 1)` 而非 `split(":")`——防止属性名本身含冒号时被误拆
2. `if 0.0 <= value <= 1.0`——超出 0~1 范围的属性值被忽略

---

### 5. 联网属性搜索

位于 `经验推广框架.py` 第 373-421 行。预测时自动触发，补充用户知识库中缺少的实体属性。

#### 为什么需要联网

```
用户只输入了 2 条关于老虎的经验 → 知识库只有老虎的属性
预测"遇到狼"时 → 狼的属性在库中无匹配 → 传播失效

联网搜索"狼的特征"后：
狼：{食肉:1.0, 群居:1.0, 有尖牙:1.0}
老虎：{食肉:1.0, 有尖牙:1.0, 攻击力:0.95}
通过共现匹配：食肉+有尖牙 → 传播攻击力到狼 ✓
```

#### 成本控制

| 策略 | 实现 | 效果 |
|------|------|------|
| 限制搜索条数 | `max_results=2` | 每次搜索只取前 2 条 |
| 限制搜索时机 | 仅 predict 时搜索 | learn 阶段不联网 |
| 限制属性数 | `max_attrs=3` | 每个实体最多补充 3 个属性 |
| 超时控制 | `timeout=5` | 网络慢时不阻塞 |

#### 降级策略

使用 DuckDuckGo（免费，无需 API Key）。任何异常（网络超时、DNS 错误、解析失败）都静默返回空字典，不影响核心推理。

```python
class WebSearcher:
    def __init__(self, llm: LLM, timeout: int = 5):
        self._ddgs = None
        if SEARCH_AVAILABLE:
            try:
                self._ddgs = DDGS(timeout=self.timeout)
            except Exception:
                self._ddgs = None  # 无网络时降级

    def get_web_attributes(self, ...) -> Dict[str, float]:
        if not self._ddgs:
            return {}  # 网络不可用时返回空
```

---

### 6. Agent 主类

位于 `经验推广框架.py` 第 428-711 行。整合所有模块。

#### 初始化参数

```python
class ExperienceAgent:
    def __init__(self, llm: LLM):
        self.kb = KnowledgeBase()
        self.extractor = Extractor(llm)
        self.normalizer = AttributeNormalizer(llm)
        self.searcher = WebSearcher(llm)
        self.user_attr_weight = 1.0    # 用户经验权重
        self.web_attr_weight = 1.0     # 联网知识权重（相同）
        self.max_propagation_dist = 0.30  # 传播距离阈值
```

#### learn——学习经验

```python
def learn(self, text: str):
```

处理流程：

```
输入文本 "在丛林里遇到老虎，老虎扑过来咬伤了我。"
  ↓  SAOR 提取
(S="我", A="遇到", O="老虎", R="被咬伤")
  ↓  属性生成（参考已有属性名）
老虎 → {食肉:1.0, 有尖牙:1.0, 攻击力:0.9}
  ↓  注册到知识库（权重 1.0）
实体入库
  ↓  更新共现图
重新计算所有属性对的共现率
  ↓  属性归一化
合并同义属性，重建共现表
  ↓  记录经验
保留完整四元组（Entity 引用）
```

#### learn_from_file——从文件批量学习

```python
def learn_from_file(self, filepath: str) -> str:
```

路径解析策略：
1. 先尝试用户提供的原样路径
2. 再尝试脚本所在目录下的路径
3. 文件名无后缀时自动尝试 `.md` → `.txt` → `.json` → `.yaml` → `.yml`

内部用 LLM 将文档内容拆成多条事件描述，逐条调用 `learn()`。

#### predict——预测

```python
def predict(self, text: str, verbose: bool = False) -> str:
def predict_stream(self, text: str, verbose: bool = False):
```

`predict` 与 `predict_stream` 共享 `_prepare_prediction` 方法，前 5 步完全一致：

```
场景描述 "我独自在野外遇到了一群狼"
  ↓  SAO 提取
(S="我", A="遇到", O="狼")
  ↓  属性生成（参考已有属性名）
我 → {独自:1.0, 无防护:1.0}
狼 → {群居:1.0, 食肉:1.0, 有尖牙:1.0}
  ↓  联网搜索补充
狼 → {领地意识强:0.9, 群体协作:0.95}
  ↓  合并属性 + 属性传播
merged: {群居:1.0, 食肉:1.0, 有尖牙:1.0, 领地意识强:0.9, 群体协作:0.95}
propagate → 攻击力高=0.67(←有尖牙, 距离=0.33), 危险=1.0(←食肉, 距离=0.00)
  ↓  构建 Prompt → LLM 推理
输出预测文本
```

`verbose=True` 时展示思考过程：

```
  ✓ 场景元素：我 → 遇到 → 狼
  ▶ 分析特征（主体/动作/客体）...
    · 我：['独自', '无防护']...
    · 遇到：['近距离', '遭遇战']...
    · 狼：['群居', '食肉', '有尖牙']...
  ▶ 联网搜索补充知识...
    · 狼 联网完毕（2 条）
  ▶ 属性传播匹配...
    · 客体 狼：6 条匹配
  ▶ 生成建议...
（LLM 输出：先列出关键属性分析，然后给出自然建议）
```

#### show_knowledge——查看知识库

```python
def show_knowledge(self) -> str:
```

返回当前知识库状态文本，包含实体数、经验数、归一化映射数、实体属性列表、属性共现表。

---

## 安装与配置

### 依赖

```bash
pip install openai python-dotenv duckduckgo_search
```

### 环境变量

打开项目目录下的 `.env` 文件，填入你的 API Key：

```
LLM_API_KEY=sk-your-key-here
LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com/v1
```

### 切换其他 LLM

本框架兼容所有 OpenAI 格式的 API，修改 `.env` 中的三行配置即可切换：

| LLM | LLM_MODEL | LLM_BASE_URL |
|-----|-----------|--------------|
| DeepSeek | deepseek-chat | https://api.deepseek.com/v1 |
| 通义千问 | qwen-plus | https://dashscope.aliyuncs.com/compatible-mode/v1 |
| 智谱 GLM | glm-4-plus | https://open.bigmodel.cn/api/paas/v4 |
| OpenAI | gpt-4o | https://api.openai.com/v1 |
| Ollama（本地） | qwen2.5:7b | http://localhost:11434/v1 |

切换示例：换用 Ollama 本地模型

```
LLM_API_KEY=ollama
LLM_MODEL=qwen2.5:7b
LLM_BASE_URL=http://localhost:11434/v1
```

---

## API 参考

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `learn(text)` | `text: str` | `None` | 从事件描述中学习经验（自动存盘） |
| `learn_from_file(path)` | `path: str` | `str` | 从文件批量学习（自动存盘） |
| `predict(text, verbose)` | `text: str, verbose: bool` | `str` | 对场景做预测，返回完整文本 |
| `predict_stream(text, verbose)` | `text: str, verbose: bool` | `Iterator[str]` | 流式预测，逐 token 产出 |
| `show_knowledge()` | 无 | `str` | 返回知识库状态文本 |

---

## 核心机制专题

### 属性传播 vs 相似度匹配

**相似度匹配**（不采用）：

```
狼 = {食肉:1, 有尖牙:1, 群居:1} → 3 个属性
老虎 = {食肉:1, 有尖牙:1, 攻击力高:1, 危险:1} → 4 个属性
similarity(狼, 老虎) = 2/5 = 0.4

问题：由于属性维度不同，相似度被拉低，0.4 的可靠度难以解释。
```

**属性传播**（采用方案）：

```
狼有"有尖牙"(值=1.0)，知识库中"有尖牙 → 攻击力高"共现率=0.67
→ 推断狼有"攻击力高"(值=0.67, 距离=0.33)

优势：
- 每个推断都有可追溯的来源属性
- 每个推断都有量化距离（可靠度指标）
- 不要求整体相似，只需要部分属性匹配
```

### 传播距离的含义

| 共现率 | 传播距离 | 含义 |
|--------|---------|------|
| 1.00 | 0.00 | 总是同时出现，完全可靠 |
| 0.90 | 0.10 | 几乎总是同时出现，基本可靠 |
| 0.67 | 0.33 | 多数情况同时出现，可参考 |
| < 0.70 | > 0.30 | 不采纳 |

距离反映的是数据的真实统计分布，不是人为设定的。

### 与直接问 LLM 的区别

```
直接问 LLM "遇到狼会怎样？"
  → LLM 靠训练语料记忆回答（统计记忆，非推理）
  → 如果狼是虚构生物，LLM 无法正确判断

使用本框架：
  1. 学习真实经验："老虎(食肉,有尖牙) → 危险"
  2. 预测"遇到狼"：提取狼的属性"食肉,有尖牙"
  3. 传播匹配："狼(食肉) → 危险"（通过共现图）
  4. LLM 基于结构化证据推理

  → 即使狼是虚构生物，只要 LLM 能提取属性，
     框架就能通过已有经验推断出结论。
```

### 权重设计

用户经验和联网搜索**权重相同**（`user_attr_weight = web_attr_weight = 1.0`），推理时不分来源，只看传播距离。

| 因素 | 说明 |
|------|------|
| 用户经验 | 属性直接来自用户提供的事件 |
| 联网搜索 | 属性来自联网补充，和用户经验同等权重 |
| 传播距离 | 距离 = 1 - 共现率，越小越可靠 |

合并规则：联网属性只在用户知识库没有时才补充，避免覆盖用户提供的硬依据。

---

## 知识库持久化

框架会自动保存知识库，**关闭程序后再次运行，之前学过的经验不会丢失**。

### 工作原理

| 时机 | 行为 |
|------|------|
| 创建 `ExperienceAgent` | 自动从 `knowledge.json` 加载历史知识库 |
| 调用 `learn()` | 学习后自动保存到 `knowledge.json` |
| 调用 `learn_from_file()` | 内部调 `learn()`，自动保存 |

### 保存内容

`knowledge.json` 包含：

- 所有实体及其属性
- 属性共现表
- 经验记录列表
- 属性归一化映射表

### 自定义路径

```python
# 默认保存在框架脚本所在目录
agent = ExperienceAgent(llm)

# 指定保存目录
agent = ExperienceAgent(llm, knowledge_dir="./my_data")
```

### 与其他模块的关系

- 两个 Agent（用户交互 Agent / 快速训练预测 Agent）共享同一个 `knowledge.json`，在一个 Agent 中学的经验，切换到另一个 Agent 也能使用
- 删掉 `knowledge.json` 即回到空白知识库状态
- `knowledge.json` 已加入 `.gitignore`，不会被上传到 GitHub

---

## 完整示例

```python
from my_llm import LLM
from 经验推广框架 import ExperienceAgent

llm = LLM()
agent = ExperienceAgent(llm)

# 输入经验
agent.learn("在丛林里遇到老虎，老虎扑过来咬伤了我。")
agent.learn("看到远处有老虎，我悄悄绕开，安全离开。")

# 从文件学习
result = agent.learn_from_file("动物知识.md")
print(result)

# 预测新场景
result = agent.predict("我独自在野外遇到了一群狼")
print(result)

# 流式预测（推荐）
for chunk in agent.predict_stream("我在山里遇到一头野猪", verbose=True):
    print(chunk, end="", flush=True)

# 查看知识库
print(agent.show_knowledge())
```

---

## 衍生 Agent

本框架提供两种开箱即用的交互方式：

- **[基于经验推广框架的用户交互 Agent](基于经验推广框架的用户交互agent.md)** — 智能对话模式，LLM 自动识别 learn/predict/chat 意图
- **[基于经验推广框架的快速训练预测 Agent](基于经验推广框架的快速训练预测agent.md)** — 非对话模式，固定流程：文件学习 → 循环预测
